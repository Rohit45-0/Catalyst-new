# 🏗️ Catalyst AI - Complete System Architecture

## 📊 System Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER JOURNEY                             │
└─────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  1. AUTHENTICATION                                               │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐      │
│  │   Signup     │───▶│    Login     │───▶│  Get Token   │      │
│  │ POST /signup │    │ POST /login  │    │ JWT Bearer   │      │
│  └──────────────┘    └──────────────┘    └──────────────┘      │
└─────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  2. PROJECT CREATION                                             │
│  ┌──────────────────────────────────────────────────┐           │
│  │  POST /projects                                   │           │
│  │  {                                                │           │
│  │    product_name: "iPhone 15 Pro",                │           │
│  │    brand_name: "Apple",                          │           │
│  │    price: "$999",                                │           │
│  │    description: "Latest flagship phone"          │           │
│  │  }                                                │           │
│  └──────────────────────────────────────────────────┘           │
└─────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  3. IMAGE UPLOAD                                                 │
│  ┌──────────────────────────────────────────────────┐           │
│  │  POST /uploads/image                              │           │
│  │  - Uploads product image                         │           │
│  │  - Stores in /uploads directory                  │           │
│  │  - Updates project.image_path                    │           │
│  └──────────────────────────────────────────────────┘           │
└─────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  4. AGENT ORCHESTRATION (The Magic Happens Here!)                │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  ORCHESTRATOR triggers sequential jobs:                 │   │
│  │                                                           │   │
│  │  Job 1: VISION_ANALYSIS                                 │   │
│  │  ├─ Input: product image                                │   │
│  │  ├─ Agent: VisionAgent                                  │   │
│  │  └─ Output: product features, colors, style             │   │
│  │                                                           │   │
│  │  Job 2: MARKET_RESEARCH                                 │   │
│  │  ├─ Input: product_name, brand, vision output           │   │
│  │  ├─ Agent: MarketResearchAgent                          │   │
│  │  └─ Output: competitors, trends, market data            │   │
│  │                                                           │   │
│  │  Job 3: INSIGHT_EXTRACTION                              │   │
│  │  ├─ Input: vision + market research                     │   │
│  │  ├─ Agent: InsightAgent                                 │   │
│  │  └─ Output: pain points, opportunities, positioning     │   │
│  │                                                           │   │
│  │  Job 4: CONTENT_GENERATION (Parallel Jobs)              │   │
│  │  ├─ LinkedIn Post                                       │   │
│  │  ├─ Ad Copy                                             │   │
│  │  ├─ Blog Article                                        │   │
│  │  └─ Video Script                                        │   │
│  │                                                           │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                   │
│  Each job:                                                       │
│  • Creates a row in `jobs` table                                │
│  • Updates status: pending → running → completed/failed         │
│  • Stores output in output_payload (JSONB)                      │
│  • Creates assets in `assets` table                             │
└─────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  5. RESULTS & ASSETS                                             │
│  ┌──────────────────────────────────────────────────┐           │
│  │  GET /projects/{id}/assets                        │           │
│  │  Returns:                                         │           │
│  │  [                                                │           │
│  │    {                                              │           │
│  │      asset_type: "linkedin_post",                │           │
│  │      content: "🚀 Introducing iPhone 15 Pro..."  │           │
│  │    },                                             │           │
│  │    {                                              │           │
│  │      asset_type: "video_script",                 │           │
│  │      content: "Scene 1: Close-up of phone..."    │           │
│  │    }                                              │           │
│  │  ]                                                │           │
│  └──────────────────────────────────────────────────┘           │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Database Schema Relationships

```
┌──────────────┐
│    users     │
│──────────────│
│ id (PK)      │◀─────────┐
│ email        │          │
│ password_hash│          │
│ is_active    │          │
└──────────────┘          │
                          │
                          │ user_id (FK)
                          │
                ┌─────────┴──────────┐
                │                    │
        ┌───────┴────────┐   ┌──────┴──────────┐
        │  user_sessions │   │    projects     │
        │────────────────│   │─────────────────│
        │ id (PK)        │   │ id (PK)         │◀────────┐
        │ user_id (FK)   │   │ user_id (FK)    │         │
        │ token          │   │ product_name    │         │
        │ expires_at     │   │ brand_name      │         │
        └────────────────┘   │ price           │         │
                             │ description     │         │
                             │ image_path      │         │
                             │ status          │         │
                             └─────────────────┘         │
                                                          │
                                                          │ project_id (FK)
                                                          │
                                        ┌─────────────────┴──────────────┐
                                        │                                │
                                ┌───────┴────────┐              ┌────────┴───────┐
                                │     jobs       │              │    assets      │
                                │────────────────│              │────────────────│
                                │ id (PK)        │              │ id (PK)        │
                                │ project_id (FK)│              │ project_id (FK)│
                                │ job_type       │              │ asset_type     │
                                │ status         │              │ content        │
                                │ input_payload  │              │ file_url       │
                                │ output_payload │              └────────────────┘
                                │ error_message  │
                                │ started_at     │
                                │ completed_at   │
                                └────────────────┘
```

---

## 🔄 Job State Machine

```
┌─────────┐
│ PENDING │  ← Job created, waiting to start
└────┬────┘
     │
     ▼
┌─────────┐
│ RUNNING │  ← Agent is processing
└────┬────┘
     │
     ├─────────────┬─────────────┐
     ▼             ▼             ▼
┌───────────┐ ┌─────────┐  ┌─────────┐
│ COMPLETED │ │ FAILED  │  │ TIMEOUT │
└───────────┘ └─────────┘  └─────────┘
     │             │             │
     ▼             ▼             ▼
  Creates      Stores        Can be
  Assets       Error         Retried
```

---

## 🧩 Code Structure

```
catalyst-ai-backend/
│
├── app/
│   ├── main.py                    # FastAPI app + CORS ✅
│   │
│   ├── api/                       # API Routes
│   │   ├── auth.py                # ✅ /signup, /login, /me
│   │   ├── projects.py            # 🟡 /projects
│   │   ├── uploads.py             # 🟡 /uploads/image
│   │   ├── jobs.py                # 🟡 /jobs
│   │   └── assets.py              # 🟡 /assets
│   │
│   ├── core/                      # Core Logic
│   │   ├── config.py              # ✅ Settings
│   │   ├── security.py            # ✅ JWT + bcrypt
│   │   └── orchestrator.py        # 🟡 Agent workflow engine
│   │
│   ├── db/                        # Database
│   │   ├── models.py              # ✅ All tables defined
│   │   └── session.py             # ✅ DB connection
│   │
│   ├── schemas/                   # Pydantic Models
│   │   ├── user.py                # ✅ UserCreate, UserOut, Token
│   │   ├── project.py             # 🟡 ProjectCreate, ProjectOut
│   │   └── job.py                 # 🟡 JobCreate, JobOut
│   │
│   └── agents/                    # AI Agents
│       ├── vision_agent.py        # 🟡 Image analysis
│       ├── research_agent.py      # 🟡 Market research
│       ├── insight_agent.py       # 🟡 Extract insights
│       └── content_agent.py       # 🟡 Generate content
│
├── uploads/                       # Uploaded images
├── .env                           # ✅ Configuration
├── requirements.txt               # ✅ Dependencies
└── database.py                    # ✅ DB initialization
```

**Legend:**
- ✅ = Implemented & Working
- 🟡 = Needs Implementation
- ❌ = Has Issues

---

## 🎯 Current Status

### ✅ COMPLETED
1. Database schema (all 5 tables)
2. Authentication system (signup, login, JWT)
3. OAuth2 password flow (Swagger compatible)
4. Session tracking
5. CORS middleware (fixes "Failed to fetch")
6. `/auth/me` endpoint

### 🟡 IN PROGRESS / TODO
1. Projects API endpoints
2. Image upload functionality
3. Agent orchestration engine
4. Individual AI agents
5. Asset generation
6. Frontend application

---

## 🚀 Quick Start Commands

```bash
# 1. Activate virtual environment
./venv/Scripts/Activate.ps1

# 2. Install dependencies
pip install -r requirements.txt

# 3. Start PostgreSQL (if not running)
# Make sure database 'catalyst_ai' exists

# 4. Start the server
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 5. Test OAuth2 fix
python test_oauth_fix.py

# 6. Open Swagger UI
# Navigate to: http://localhost:8000/docs
```

---

## 🔐 Security Notes

- JWT tokens expire in 30 minutes
- Passwords hashed with bcrypt
- Sessions tracked in database
- Token revocation ready (via user_sessions table)
- CORS currently allows all origins (change for production)

---

**Last Updated:** 2026-01-26
**Status:** OAuth2 Error FIXED ✅
